{
  "name": "gas-price-tracker",
  "version": "0.2.1",
  "private": true,
  "type": "module",
  "scripts": {
    "scan": "bun run src/index.ts",
    "aggregate": "bun run src/aggregate.ts",
    "aggregate-all": "bun run src/aggregateAll.ts",
    "aggregate-senders": "bun run src/aggregateSenders.ts",
    "fill-gaps": "bun run src/fillGaps.ts",
    "collect-batcher": "bun run src/batcherCollector.ts",
    "serve": "bun run src/serve.ts",
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "screenshot": "bun run scripts/screenshot.ts"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "@types/bun": "^1.3.14",
    "playwright": "^1.60.0",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "@atlas-chain/sdk": "0.6.12",
    "commander": "^15.0.0",
    "viem": "^2.38.2"
  }
}
