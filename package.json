{
  "name": "gas-price-tracker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "scan": "bun run src/index.ts",
    "serve": "bun run src/serve.ts",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "typescript": "^5.9.3"
  }
}
